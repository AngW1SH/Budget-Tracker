import { describe, test } from "vitest";

describe("User Month Calendar UI-widget", () => {
  test("temp", () => {});
  /*
  test("snapshot UI testing", () => {
    vi.useFakeTimers().setSystemTime(new Date("2023-08-01T00:00:00"));

    const wrapper = mount(UserMonthCalendar);

    expect(wrapper).toMatchSnapshot();
  }); */
});
